<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo">
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.ChangeAddress" android:label="Change Address"></activity>
        <activity android:name=".ui.DisplayAddress" android:label="Address" />
        <activity android:name=".ui.AddAddress" android:label="Add Address" />
        <activity
            android:name=".ui.SendMoney"
            android:label="Send Money" />
        <activity
            android:name=".ui.AddMoney"
            android:label="Add Money" />
        <activity android:name=".ui.MyAddress" />
        <activity
            android:name=".ui.ChangePassword"
            android:label="Change Password" />
        <activity
            android:name=".ui.Profile"
            android:label="Profile" />
        <activity
            android:name=".Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Register"
            android:label="Register" />
        <activity
            android:name=".Login"
            android:label="Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>